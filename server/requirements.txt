# Pinned loosely so the same file resolves on x86-64 (laptop) and arm64 (Pi).
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
websockets>=14.0,<18.0
httpx>=0.27,<1.0
opuslib==3.0.1
edge-tts>=7.0,<8.0
python-dotenv>=1.0,<2.0
